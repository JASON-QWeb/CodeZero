---
name: frontend-verification
version: 0.1.0
---

# Frontend Verification

Frontend tasks must pass build, lint, typecheck when available, unit tests, and Chrome screenshot checks.

Capture desktop and mobile screenshots when URLs are configured. Console errors are blocking unless explicitly waived by a human.

